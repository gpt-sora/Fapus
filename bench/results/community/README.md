# Community results

PR your scored runs here: one directory per submission
(`<github-user>-<date>/`) containing your `results.csv` (same columns as
`../results.csv`), the raw run dirs (or a link if huge), and a line stating
model versions, harness, and whether you used an isolated
`CLAUDE_CONFIG_DIR`. See CONTRIBUTING.md at the repo root.
